"use client";

/**
 * Deprecated — live telemetry is owned by `useWaferRealtime`.
 * Kept as a no-op export so older imports do not break.
 */
export function useLiveTelemetry(_enabled = true) {
  void _enabled;
}
